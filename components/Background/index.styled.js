import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
  },

  background: {
    flex: 1,
    width: "100%",
    height: "100%",
  },
});

export { styles as default };
